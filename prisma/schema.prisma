generator client {
  provider = "prisma-client-js"
  output   = "generated"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Vault {
  uuid   String @id @default(uuid())
  label  String @db.VarChar(255)
  secret String
}
